# SmartTicket 环境变量模板
# 复制为 .env 并填入真实值（.env 已在 .gitignore 中排除，不会被提交）

# JWT
JWT_SECRET=smartticket-jwt-secret-key-2026-min-256-bits!!

# MySQL
MYSQL_PASSWORD=root

# PostgreSQL
PG_PASSWORD=root

# LLM API（可选，不填则使用内置 Mock 模式）
LLM_API_KEY=sk-your-key-here
LLM_BASE_URL=https://api.openai.com/v1
